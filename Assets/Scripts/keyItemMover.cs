using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using DG.Tweening;

public class keyItemMover : MonoBehaviour
{
    // keyitemをくるくるぴょんぴょんさせたいです
    // atacchしたオブジェクトが変に移動する...

    // Start is called before the first frame update
    void Start()
    {
        // this.transform.DORotate(new Vector3(0f, 90f), 1f);
        // this.transform.DOJump(endValue: new Vector3(5.0f, 0.5f, 0), jumpPower: 5.0f, numJumps: 2, duration: 2.0f);
    }

    // Update is called once per frame
    void Update()
    {

    }
}
